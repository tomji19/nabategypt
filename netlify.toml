[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: React Router paths must serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
